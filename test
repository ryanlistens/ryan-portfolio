.arcade-zone {
  display:flex;
  justify-content:center;
  margin:4rem 0;
}

.arcade-machine {
  width:260px;
  background:#111;
  border:6px solid #000;
  text-decoration:none;
  color:#fff;
  box-shadow:0 40px 80px rgba(0,0,0,.9);
  transition:.3s ease;
}

.arcade-machine:hover {
  transform:translateY(-6px);
  box-shadow:0 50px 120px rgba(255,80,80,.4);
}

/* MARQUEE */

.marquee {
  height:70px;
  background:#000;
  display:flex;
  align-items:center;
  justify-content:center;
  position:relative;
  border-bottom:4px solid #000;
}

.marquee img {
  height:46px;
  z-index:2;
}

.marquee-glow {
  position:absolute;
  inset:0;
  background:radial-gradient(circle,rgba(255,180,80,.35),transparent 70%);
  animation:glow 3s infinite alternate;
}

@keyframes glow {
  from{opacity:.5}
  to{opacity:1}
}

/* SCREEN */

.bezel {
  padding:14px;
  background:#000;
}

.crt {
  height:180px;
  background:#050505;
  position:relative;
  overflow:hidden;
  border-radius:6px;
  box-shadow:inset 0 0 40px rgba(0,0,0,.9);
}

.screen-art {
  width:100%;
  height:100%;
  object-fit:cover;
  filter:brightness(1.2) contrast(1.1);
}

.scanlines {
  position:absolute;
  inset:0;
  background:repeating-linear-gradient(
    to bottom,
    rgba(0,0,0,.25),
    rgba(0,0,0,.25) 1px,
    transparent 2px
  );
}

.glass {
  position:absolute;
  inset:0;
  background:
    radial-gradient(circle at 30% 20%,rgba(255,255,255,.18),transparent 60%),
    linear-gradient(transparent,rgba(0,0,0,.4));
}

/* CONTROLS */

.control-panel {
  height:120px;
  background:linear-gradient(#2a2622,#14110f);
  border-top:4px solid #000;
  display:flex;
  align-items:center;
  justify-content:space-around;
}

.joystick {
  width:38px;
  height:38px;
  border-radius:50%;
  background:radial-gradient(#ff5555,#770000);
  box-shadow:
    inset -2px -2px 4px rgba(255,255,255,.2),
    0 4px 0 #000;
}

.buttons span {
  display:inline-block;
  width:28px;
  height:28px;
  margin-left:6px;
  border-radius:50%;
  background:radial-gradient(#55ff55,#117711);
  box-shadow:
    inset -2px -2px 4px rgba(255,255,255,.2),
    0 4px 0 #000;
}

/* COIN */

.coin-door {
  height:70px;
  background:#0a0a0a;
  border-top:4px solid #000;
  text-align:center;
  padding-top:8px;
}

.coin-text {
  font-family:Arial;
  font-size:9px;
  letter-spacing:1px;
  color:#666;
}

.slot {
  width:40px;
  height:6px;
  background:#000;
  margin:6px auto;
}

.return {
  width:20px;
  height:20px;
  border-radius:50%;
  border:2px solid #000;
  margin:0 auto;
}

/* BASE */

.kickplate {
  height:40px;
  background:#000;
}

<div class="arcade-zone">
  <a href="https://mullet.pro/game" class="arcade-machine">

    <div class="marquee">
      <div class="marquee-glow"></div>
      <img src="assets/mullet-marquee.png" alt="Mullet Pro">
    </div>

    <div class="bezel">
      <div class="crt">
        <img src="assets/mullet-title.png" class="screen-art" alt="Game screen">
        <div class="scanlines"></div>
        <div class="glass"></div>
      </div>
    </div>

    <div class="control-panel">
      <div class="joystick"></div>
      <div class="buttons">
        <span></span><span></span><span></span>
      </div>
    </div>

    <div class="coin-door">
      <div class="coin-text">INSERT COIN</div>
      <div class="slot"></div>
      <div class="return"></div>
    </div>

    <div class="kickplate"></div>

  </a>
</div>
